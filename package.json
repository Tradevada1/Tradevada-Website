{
  "name": "tradevada-website",
  "private": true,
  "scripts": {
    "build:blog": "node scripts/build-blog.js",
    "deploy": "node scripts/build-blog.js && wrangler deploy && node scripts/indexnow.js",
    "indexnow": "node scripts/indexnow.js"
  },
  "devDependencies": {
    "marked": "^15.0.0"
  }
}
